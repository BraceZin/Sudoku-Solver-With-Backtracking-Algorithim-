# Sudoku-Solver-With-Backtracking-Algorithim-
A backtracking algorithim is used to solve sudoku. Currently, a GUI is being worked on so there is a interface. But, you can change the nested array into numbers.
